import 'package:flutter/material.dart';

class AccountSreen extends StatelessWidget {
  const AccountSreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(
          child: Text("file screen"),
        ),
      ),
    );
  }
}
